<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:deviceSettingsViewModels ="clr-namespace:NetOptimizer.ViewModels.DeviceSettingss"
                    xmlns:converters="clr-namespace:NetOptimizer.Convertors"
                    xmlns:selectors="clr-namespace:NetOptimizer.Views.Selectors">
    <!-- =====================Конверторы==================== -->
    <converters:DeviceTypeToIconConverter x:Key="DeviceTypeToIconConverter"/>
    <converters:DeviceTypeToAbstractNameConverter x:Key="DeviceTypeToAbstractNameConverter"/>
    <!-- ===================== Стили ==================== -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="#EFEFEF"/>
        <Setter Property="Foreground" Value="#333"/>
        <Setter Property="Padding" Value="5,2"/>
    </Style>
    
    <Style x:Key="ModernTabItemStyle" TargetType="TabItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="headerBorder" 
                        Background="Transparent" 
                        BorderThickness="0,0,0,2" 
                        BorderBrush="Transparent"
                        Cursor="Hand"
                        Padding="10,5">
                        <ContentPresenter x:Name="contentPresenter" 
                                      ContentSource="Header" 
                                      RecognizesAccessKey="True"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="headerBorder" Property="Background" Value="White"/>
                            <Setter TargetName="headerBorder" Property="BorderBrush" Value="#4A89F3"/>
                            <Setter TargetName="headerBorder" Property="BorderThickness" Value="0,0,0,3"/>
                            <Setter Property="Foreground" Value="#4A89F3"/>
                        </Trigger>

                        <Trigger SourceName="headerBorder" Property="IsMouseOver" Value="True">
                            <Setter TargetName="headerBorder" Property="Background" Value="#F5F5F5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="8,0,0,0"/>
        <Setter Property="Margin" Value="0,5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <BulletDecorator Background="Transparent">
                        <BulletDecorator.Bullet>
                            <Border x:Name="Box" 
                                    Width="15" Height="15" 
                                    CornerRadius="9" 
                                    BorderThickness="1" 
                                    BorderBrush="Gray" 
                                    Background="White">
                            </Border>
                        </BulletDecorator.Bullet>
                        <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                    </BulletDecorator>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="BorderThickness" Value="5"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="#5B7DFA"/>
                            <Setter TargetName="Box" Property="Background" Value="White"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="#5B7DFA"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Background="{TemplateBinding Background}">
                        <Border x:Name="TrackBackground" 
                            Height="4" 
                            Margin="5,0"
                            Background="#D3D3D3" 
                            CornerRadius="2" 
                            VerticalAlignment="Center"/>

                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                    <RepeatButton.Style>
                                        <Style TargetType="RepeatButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RepeatButton">
                                                        <Border Background="#4A89F3" Height="4" CornerRadius="2" />
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RepeatButton.Style>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>

                            <Track.Thumb>
                                <Thumb x:Name="Thumb" Width="10" Height="10" Focusable="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse Fill="#4A89F3" StrokeThickness="0">
                                                <Ellipse.Effect>
                                                    <DropShadowEffect BlurRadius="4" Opacity="0.2" ShadowDepth="1"/>
                                                </Ellipse.Effect>
                                            </Ellipse>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>

                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                    <RepeatButton.Style>
                                        <Style TargetType="RepeatButton">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="RepeatButton">
                                                        <Border Background="Transparent" Height="4" />
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </RepeatButton.Style>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="Thumb" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="MinWidth" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid x:Name="Bg" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                        <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Opacity="0" Focusable="false"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Opacity="0" Focusable="false"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Margin="2,0">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#CCC" CornerRadius="3" />
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width" Value="Auto"/>
                            <Setter Property="Height" Value="8"/>
                            <Setter Property="MinHeight" Value="8"/>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            <Setter Property="Margin" Value="0,2" TargetName="PART_Track"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#333"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="Bg" 
                        Background="{TemplateBinding Background}" 
                        BorderThickness="1"
                        BorderBrush="Transparent"
                        CornerRadius="5" 
                        SnapsToDevicePixels="True"
                        Margin="2,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter x:Name="IconPresenter"
                                          Grid.Column="0"
                                          ContentSource="Icon"
                                          Width="18" Height="18"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>

                            <ContentPresenter Grid.Column="1"
                                          ContentSource="Header"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>

                            <Popup x:Name="PART_Popup" 
                               AllowsTransparency="True" 
                               IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                               Placement="Right" 
                               PopupAnimation="Fade">
                                <Border Background="White" 
                                    BorderBrush="#DDD" 
                                    BorderThickness="1" 
                                    CornerRadius="5" 
                                    Padding="2"
                                    Margin="0,0,5,5">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" Opacity="0.15" ShadowDepth="2" Color="Black"/>
                                    </Border.Effect>

                                    <ScrollViewer MaxHeight="300" 
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#F0F0F0"/>
                            <Setter TargetName="Bg" Property="BorderBrush" Value="#DCDCDC"/>
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#E8E8E8"/>
                            <Setter TargetName="Bg" Property="BorderBrush" Value="#CCCCCC"/>
                        </Trigger>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="IconPresenter" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ActionBtn" TargetType="Button">
        <Setter Property="Background" Value="#EFEFEF"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="0,5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="3"/>
            </Style>
        </Style.Resources>
    </Style>

    <Style x:Key="RemoveGroupButton" TargetType="Button">
        <Setter Property="Foreground" Value="Gray"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">

                            <Setter TargetName="btnBorder" Property="Background" Value="#CF2B13"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="btnBorder" Property="Background" Value="#B60000"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =====================ДАТА ТЕМПЛЕЙТЫ ==================== -->
    
    <DataTemplate DataType="{x:Type deviceSettingsViewModels:PcSubMenuViewModel}">
        <StackPanel>
            <CheckBox Content="Учитывать в бюджете"
                  IsChecked="{Binding IncludeInBudget, Mode=TwoWay}" 
                  Margin="10,10,0,0"/>
        </StackPanel>
    </DataTemplate>
    
    <!--Шаблон элементов со слайдерами-->
    <DataTemplate x:Key="DeviceWithCountTemplate">
        <Border BorderBrush="#CECECE" BorderThickness="1" Padding="10,0,10,0" CornerRadius="5" Margin="0,0,0,5">
            <StackPanel Margin="0,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="25"/>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="25"/>
                    </Grid.ColumnDefinitions>

                    <Image Source="{Binding Type, Converter={StaticResource DeviceTypeToIconConverter}}" Width="26" Height="26"/>
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="Black" Text="{Binding Type, Converter={StaticResource DeviceTypeToAbstractNameConverter}}" Margin="5,0,0,0"/>
                </Grid>
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                        <ItemsControl ItemsSource="{Binding Allocations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" 
                                                BorderBrush="#EDEDED" BorderThickness="1" CornerRadius="10" 
                                                Padding="15,10" Background="Transparent">
                                            <DockPanel LastChildFill="True">
                                                <TextBlock Text="{Binding Group.GroupName}" VerticalAlignment="Center" 
                                                           FontSize="8" Foreground="#373737" DockPanel.Dock="Left"  Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Count}"  VerticalAlignment="Center" Foreground="Gray"  FontSize="16" 
                                                           DockPanel.Dock="Right"  Margin="10,0,0,0"/>
                                                
                                                <Slider Minimum="0"  Maximum="1000" Value="{Binding Count, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                        VerticalAlignment="Center"  IsMoveToPointEnabled="True"/>
                                            </DockPanel>
                                        </Border>

                                        <Button Grid.Column="1" Command="{Binding DataContext.editorViewModel.DisconnectFromGroupCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                CommandParameter="{Binding}"  Width="40" Height="35" Cursor="Hand" Margin="8,0,0,0" VerticalAlignment="Center">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="btnBorder" Background="White" CornerRadius="8" 
                                                            BorderThickness="1" BorderBrush="#EDEDED" SnapsToDevicePixels="True">
                                                        <Image Source="/Assets/Images/disconnectGroup.png" Width="22" Height="22" 
                                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="btnBorder" Property="BorderBrush" Value="#FFC1C1"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Grid Grid.Row="1" Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition Width="40" />
                        </Grid.ColumnDefinitions>

                        <Rectangle Grid.Column="0" IsHitTestVisible="False" Stroke="Gray" Height="32" 
                                   StrokeThickness="0.5" StrokeDashArray="4 4" 
                                   RadiusX="3" RadiusY="5"
                                   SnapsToDevicePixels="True"
                                   HorizontalAlignment="Stretch"/>

                        <Button Grid.Column="2" 
                                Command="{Binding DataContext.editorViewModel.OpenConnectGroupWindowCommand,
                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"
                                Width="40" Height="35" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="btnBorder" Background="White" CornerRadius="6" 
                                            BorderThickness="1" BorderBrush="#EDEDED" SnapsToDevicePixels="True">
                                        <Image Source="/Assets/Images/connectedGroup.png" Width="25" Height="25" 
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="btnBorder" Property="BorderBrush" Value="#87FF7A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            
                        </Button>
                    </Grid>
                </Grid>

                <Border x:Name="SettingsArea"  Background="#F9FBFF" 
                        CornerRadius="5"  Margin="0,5,0,0"
                        BorderBrush="#E0E7FF" BorderThickness="1" MinHeight="65">

                    <StackPanel>
                        <TextBlock Text="Дополнительные параметры:" FontSize="10"
                                   Foreground="#9CA3AF" Margin="10,5,0,0"/>
                        <ContentControl Content="{Binding DeviceSubMenuViewModel}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </DataTemplate>
    <!--Шаблон элементов без слайдеров-->
    <DataTemplate x:Key="DeviceWithoutCountTemplate">
        <Border BorderBrush="#CECECE" BorderThickness="1" Padding="10,0,10,0" CornerRadius="5" Margin="0,0,0,5">
            <StackPanel Margin="0,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="25"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Image Source="{Binding Type, Converter={StaticResource DeviceTypeToIconConverter}}" Width="26" Height="26"/>

                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="Black"
                               Text="{Binding Type, Converter={StaticResource DeviceTypeToAbstractNameConverter}}" 
                               Margin="5,0,0,0"/>
                </Grid>
                <Border x:Name="SettingsArea"  Background="#F9FBFF" 
                        CornerRadius="1"  Margin="10"
                        BorderBrush="#E0E7FF" BorderThickness="1">
                    <StackPanel>
                        <TextBlock TextAlignment="Center" Text="Дополнительные настройки:" FontSize="10" Foreground="#9CA3AF" Margin="0,0,0,5"/>
                        <ContentControl Content="{Binding DeviceSubMenuViewModel}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </DataTemplate>

<!--ШАБЛОН СПИСКА ГРУПП-->
    <DataTemplate x:Key="DeviceGroupItemTemplate">
        <Border BorderBrush="#E1E1E1" BorderThickness="1" CornerRadius="6"
                Margin="0,0,5,5" Padding="10,2" Height="25" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
 
                <TextBlock Text="{Binding GroupName}" FontSize="11"  Foreground="Black"
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>

                <Button Style="{StaticResource RemoveGroupButton}" Command="{Binding DataContext.editorViewModel.RemoveDeviceGroupCommand, 
                                                                             RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding}" Content="" FontFamily="Segoe MDL2 Assets"  FontSize="10"
                        Width="15" Height="15" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,5,0"
                        Cursor="Hand">
                </Button>
            </Grid>
        </Border>
    </DataTemplate>
    <selectors:DeviceTemplateSelector x:Key="DeviceTemplateSelector"
                               WithCountTemplate="{StaticResource DeviceWithCountTemplate}"
                               WithoutCountTemplate="{StaticResource DeviceWithoutCountTemplate}"/>
</ResourceDictionary>