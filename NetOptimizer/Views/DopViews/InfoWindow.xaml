<Window x:Class="NetOptimizer.Views.DopViews.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:NetOptimizer.Views.DopViews"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Title="ErrorWindow" Height="150" Width="208">
    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                         Background="{TemplateBinding Background}"
                         BorderBrush="{TemplateBinding BorderBrush}"
                         BorderThickness="{TemplateBinding BorderThickness}"
                            
                         CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
        CaptionHeight="0"
        CornerRadius="16"
        GlassFrameThickness="0"
        ResizeBorderThickness="0"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Rectangle MouseLeftButtonDown="NavBar_MouseLeftButtonDown" Width="272" Fill="#F7F7F7" Height="27" VerticalAlignment="Top" HorizontalAlignment="Center"></Rectangle>
        <Image Source="/Assets/Images/titleicon.png" VerticalAlignment="Top" HorizontalAlignment="Left" Width="31" Height="28" Margin="-3,-1,0,0"></Image>
        <Label Foreground="Black" Content="Уведомление!" FontSize="10" VerticalAlignment="Top" Margin="25,1,111,0" ></Label>

        <Image Source="/Assets/Images/Info.png" Width="50" Height="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="11,0,0,0"></Image>
        <TextBox Text="{Binding Message}"  BorderThickness="0" Width="121" Height="70" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="77,0,0,0" HorizontalContentAlignment="Center" TextWrapping="Wrap"></TextBox>
        <Rectangle Width="270" Stroke="Black" StrokeThickness="0.3" Height="35" Fill="#F1F1F1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,210,0,0"></Rectangle>
        <Button Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource RoundedButtonStyle}" Cursor="Hand" Background="white" Foreground="Gray" BorderThickness="0.5" FontSize="10"  Content="OK" Width="50" Height="30" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,10,2"></Button>
    </Grid>
</Window>
